# _collection.yaml — auto-generated by migrate-notion.mjs
# Review and adjust before use.
#
# view:     list | table | gallery
# group_by: <field>   # e.g. saison — renders as tabbed groups
# sort_by:  <field>   # e.g. compteur_d_ep
# sort_order: asc | desc
view: list
# group_by: ~
# sort_by: ~
# sort_order: asc
fields:
  - title
  - description
  - saisons
